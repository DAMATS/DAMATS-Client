<div class="panel-heading">
    <button type="button" class="close"><i class="fa fa-times-circle"></i></button>
    <h3 class="panel-title"><i class="fa fa-fw fa-user"></i>&nbsp;User Profile</h3>
</div>
<div class="panel-body">
{{#if is_fetching }}
  <p class="text-danger" id="alert">
    <i class="fa fa-cog fa-spin"></i> Loading user profile ...
  </p>
{{else}}{{#if fetch_failed }}
  <p class="text-danger" id="alert">
    <i class="fa fa-warning"></i> Failed to load the user profile from the server!
  </p>
{{else}}
  <h4 {{#if description }}title="{{ description }}"{{/if}}><i class="fa fa-fw fa-user"></i>&nbsp;{{#if name }}{{ name }} ({{ identifier }}){{ else }}{{ identifier}}{{/if}}</h4>
{{#if is_empty }}
  <p class="text-danger" id="alert">
    The user is not member of any group.
  </p>
{{else}}
  <p>The user is member of following groups:</p>
  <ul class="list-group user-group-list" id="user-group-list"></ul>
{{/if}}{{/if}}{{/if}}
<div class="panel-footer">
  <div class="form-horizontal">
    <div class="form-group">
      <label for="txt-name" class="col-sm-2 control-label">Name</label>
      <div class="col-sm-10">
        <input id="txt-user-name" type="text" class="form-control" placeholder="user's name">
      </div>
    </div>
    <div class="form-group">
      <label for="txt-description" class="col-sm-2 control-label">Info</label>
      <div class="col-sm-10">
        <textarea id="txt-user-description" class="form-control" rows="4" placeholder="user's description"></textarea>
      </div>
    </div>
  </div>
  <button class="btn btn-block" id="btn-user-update"  href="#modal-user-update" title="Save changed profile to the server."><i class="fa fa-fw fa-save"></i>&nbsp;Upload changes</button>
</div>
